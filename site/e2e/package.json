{
  "name": "ledge-e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Gate 8: smoke tests against the built static export. Kept out of site/package.json on purpose — these run against `out/`, not against modules, so they need a build and a server and have no business slowing `npm test` down.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
